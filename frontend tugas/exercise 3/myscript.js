// create variable header
let header = (
    <header className="header">
        <h1>Buat header Menggunakan JSX</h1>
        <p>Tutotrial JSX Sederhana</p>
    </header>
);

//render elemen ke DOM 
ReactDOM.render(header, document.getElementById("app"));